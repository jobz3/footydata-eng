version: 2
models:
  - name: standings_pl
    tests:
    - dbt_utils.unique_combination_of_columns:
        arguments:
          combination_of_columns:
            - competition_code
            - team_name   